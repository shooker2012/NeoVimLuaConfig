# nvim-config-modernization

将现有 Neovim 配置迁移到 nvim-treesitter main、Neovim 0.11 API 与按需加载架构。

本目录中的 `proposal.md`、`design.md`、`specs/` 与 `tasks.md` 是当前唯一实施依据；`.kiro/specs/nvim-config-modernization/` 仅保留为只读历史输入，不再同步修改其正文。

基线摘要位于 `baselines/`。架构决策位于 `docs/adr/`，项目术语定义位于仓库根目录 `CONTEXT.md`。
