# 任务 7.1 Neovim 可观察行为人工验收结果

- 任务：`7.1 Checkpoint - API 迁移与格式化`
- 日期：`2026-08-13`
- Neovim：`0.11.6`
- 系统：`Windows（Asia/Shanghai）`
- 结果：`通过`

## 自动化结果

- `verify.lua`：`PASS: 68 assertions`
- 启动诊断：未检测到 `E<number>`、`Error` 或 `deprecated`
- 启动计时：丢弃预热后的中位数为 `294.753ms`
- `:Format`：启动期存在、支持范围；首次执行按需加载 conform
- 外部 formatter：本机未安装 `clang-format`、`stylua`、`ruff`、`black`；缺失时已确认输出可读通知且无 Lua 堆栈
- null-ls/Copilot：实现代码、插件声明、lockfile 和人工测试入口均已移除；本机 lazy 缓存未主动删除

## 人工验收结果

- 用户已在 Neovim 中打开相关 C++、Lua、Python、Rust 文件，并于 `2026-08-13` 确认所有可执行验收项通过。
- `:Format` 与 `:'<,'>Format` 可调用，首次执行没有 Lua 堆栈；范围命令保留选区调用语义。
- 本机缺少独立 formatter 时，通知内容可读且不会中断编辑；按任务约定，C++、Lua、Python 的实际外部格式化结果记为环境未覆盖，不构成失败。
- Rust LSP、三个 LSP client 及 LSP 交互均按测试说明通过。
- 诊断跳转、`K`、`<C-k>` 与 `<Leader>e` 已在能稳定产生诊断的实际文件中验证；浮窗使用 rounded 边框，未观察到错位。

后续说明：最终 11.1 全量视觉回归覆盖更多浮窗后，自动化环境另起的 conhost 中所有 rounded 边框均错位；用户同时观察到其他启动有时正常。进程树显示两次启动使用了不同终端宿主，最终实现保留 rounded，并以用户日常 Windows Terminal profile 中的 11.1 复验结果为准。

## 结论

自动化检查与 Neovim 可观察行为人工验收均通过，任务 7.1 检查点完成，可以进入下一阶段。
