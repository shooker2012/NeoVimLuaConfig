# 任务 11.1 Neovim 可观察行为人工验收结果

- 任务：`11.1 Final checkpoint - 全量验证与清理`
- 日期：`2026-08-13`
- Neovim：`0.11.6`
- 系统：`Windows 10 Enterprise（Asia/Shanghai）`
- 结果：`通过`

## 自动化结果

- `verify.lua`：`PASS: 122 assertions`
- 启动诊断：未检测到 `E<number>`、`Error` 或 `deprecated`
- 5 轮启动计时：恢复 rounded 边框后丢弃预热的中位数为 `131.613ms`
- 性能对比：比 `367.068ms` 可执行基线快 `235.455ms`（`64.1%`），达到 `200ms` 尽力目标
- VeryLazy：补充工作在独立 headless 模拟中约 `143.057ms`，并单独报告延迟加载清单
- health：未新增 ERROR 或 deprecated；`nvim-treesitter` 仍报告基线已有的 `site/ is not in runtimepath`。实际目录已位于 runtimepath，parser/highlight 正常；这是其 Windows health 将 `C:/.../site/` 与 `C:\...\site` 直接比较造成的路径表示误报
- 外部 formatter：`clang-format`、`stylua`、`ruff`、`black` 仍属于独立环境依赖；缺失时可读通知为硬门禁，实际格式化成功允许记为未验证

## 人工确认

- 首次人工验收：用户确认除 rounded 边框外其他项目均通过；自动化环境直接启动的窗口中所有 rounded 浮窗均出现错位。
- 后续观察：用户发现某些启动中的 rounded 边框正常，希望保留 rounded。进程树确认异常验收窗口由 `Start-Process nvim` 另起传统 `conhost.exe`，而用户日常终端为 Windows Terminal；两者使用不同文本测量/字体渲染路径。
- 处理：恢复全局 `rounded`，不把宿主相关的字符渲染问题误当成 Neovim 配置问题；最终视觉验收改为从用户日常 Windows Terminal profile 启动。
- 最终复验：用户在日常 Windows Terminal 中确认 `K`、`<C-k>`、`<Leader>e`、Telescope、Neo-tree 和 Hop 的 rounded 边框均正常，其余人工项目此前已全部通过。

任务 11.1 的自动化、性能、health 与人工视觉验收均已完成。
