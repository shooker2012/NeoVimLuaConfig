# Neovim 配置现代化词汇表

本文只定义本项目文档中的领域术语。实施要求与验收标准以 `openspec/changes/nvim-config-modernization/` 为准。

**可执行基线**：在同一台机器上，用当前变更内的固定脚本和参数可重复得到的改造前快照，用于判断本次迁移是否造成回归。

**历史参考值**：过去记录、但无法由当前验证流程直接复现的测量值；只用于理解趋势，不参与回归判定。

**阶段 Checkpoint**：阶段完成前的硬门禁。自动化、启动、目标行为和回归检查中的真实失败会阻止推进；只有外部环境不可用导致的检查才能标记为“未验证”。

**可观察行为人工验收包**：用于验证能通过启动 Neovim、打开代表性文件并进行编辑器内操作直接观察到的效果，包含操作说明、测试输入和结果记录。纯规格、Git 基线、验证脚本、静态断言、内部状态、性能报告与文档不生成此类验收包。

**等待依赖**：某项 Neovim 可观察行为的人工验收依赖尚未完成，因而暂不生成或执行该验收的状态。

**非预期全局写入**：配置在未记录为跨模块共享状态的情况下，向 `_G` 创建或覆盖项目级名称。

**有意共享状态**：为跨模块协作而明确保留、具有单一语义和已知读写方的 `_G` 状态。本项目当前唯一承认的项目级共享状态是 `_G.sal_diagnostic_severity`：由 `init.lua` 初始化，由诊断跳转按键读取，并由 `:SetDiagnosticSeverity` 读写。
